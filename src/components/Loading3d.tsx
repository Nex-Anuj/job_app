import { Html } from "@react-three/drei"

const Loading3d = () => {
  return (
    <Html center>
      <div className="h-[100vh] w-[100vw] flex items-center justify-center bg-[#0a0a0a] text-[#D91099] font-black tracking-[10px] animate-pulse">LOADING...</div>;
    </Html>
  )
}

export default Loading3d
